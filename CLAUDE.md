# Food Recorder (ثبت غذا)

اپلیکیشن وب یک‌صفحه‌ای (PWA) برای ثبت غذاهای مصرف‌شده/تهیه‌شده، به زبان فارسی و راست‌به‌چپ. بدون بک‌اند — همه‌چیز سمت کلاینت و در `localStorage` مرورگر ذخیره می‌شود.

## ۱. معماری

### ساختار فایل‌ها
| فایل | نقش |
|---|---|
| `index.html` | تنها صفحه‌ی اپ؛ شامل مارک‌آپ splash، لیست غذاها، تب آمار، مودال ثبت/ویرایش و مودال تنظیمات |
| `script.js` | تمام منطق اپ: state، رندر UI، ذخیره‌سازی، ثبت Service Worker، چک آپدیت، بک‌آپ |
| `style.css` | استایل کامل اپ |
| `sw.js` | Service Worker — کش آفلاین و استراتژی fetch |
| `manifest.json` | مانیفست PWA (نام، آیکون‌ها، `display: standalone`) |
| `version.json` | فقط شامل `{ version, updatedAt }`؛ توسط `checkUpdate()` از شبکه (بدون کش) خوانده می‌شود تا وجود نسخه‌ی جدید تشخیص داده شود |
| `icon-*.png`, `logo.png` | آیکون‌های PWA و لوگوی splash |

اپ کاملاً استاتیک است؛ هیچ build step یا bundler ندارد — فایل‌ها مستقیم توسط مرورگر اجرا می‌شوند.

### کلیدهای localStorage
همه از طریق `getData(key, def)` / `setData(key, val)` در `script.js` خوانده/نوشته می‌شوند (سریالایز JSON):

| کلید | محتوا |
|---|---|
| `foods` | آرایه‌ی رکوردهای غذا (`{ id, name, group, meal, date, maker, serving }`) — منبع اصلی داده‌ی کاربر |
| `groups` | آرایه‌ی نام گروه‌های غذا (پیش‌فرض از `DEFAULT_GROUPS`) |
| `groupColors` | آبجکت نگاشت نام گروه → کد رنگ (پیش‌فرض از `DEFAULT_GROUP_COLORS`) |

بک‌آپ/بازیابی (`exportBackup` / `importBackup`) یک فایل JSON شامل همین سه بخش (حداقل `foods` و `groups`) تولید/می‌خواند. `clearAllData` هر سه کلید را ریست می‌کند (`foods` خالی، `groups`/`groupColors` به پیش‌فرض).

### استراتژی کش (Service Worker)
- `sw.js` روی `install` تمام `CORE_ASSETS` (index.html، style.css، script.js، آیکون‌ها، manifest.json) را در کشی به نام `food-recorder-<CACHE_VERSION>` ذخیره می‌کند.
- استراتژی fetch برای همه‌ی فایل‌های اصلی: **Network First** — همیشه اول از شبکه می‌گیرد و کش را به‌روزرسانی می‌کند؛ فقط در حالت آفلاین (fetch fail) به کش برمی‌گردد.
- `version.json` از این قاعده مستثناست و همیشه مستقیم و بدون کش (`cache: "no-store"`) از شبکه خوانده می‌شود، چون مبنای تشخیص نسخه‌ی جدید در `checkUpdate()` است.
- روی `activate`، هر کش قدیمی که نامش با `CACHE_NAME` فعلی یکی نباشد حذف می‌شود (`caches.keys()` + `filter` + `delete`).
- پیام `SKIP_WAITING` از صفحه به SW باعث فعال‌سازی فوری نسخه‌ی جدید می‌شود.

## ۲. نقاط نسخه — باید همیشه هم‌زمان آپدیت شوند

نسخه‌ی اپ در **سه جای مجزا** نگهداری می‌شود و هر سه باید در هر تغییر نسخه، دقیقاً هم‌زمان و با همان عدد به‌روزرسانی شوند:

1. `script.js` → `const APP_VERSION = "X.Y";` (خط ۲)
2. `sw.js` → `const CACHE_VERSION = "vX.Y";` (نام کش را تعیین می‌کند و باعث پاک‌شدن کش قدیمی روی `activate` می‌شود)
3. `version.json` → فیلد `"version": "X.Y"` (پایه‌ی مقایسه در دکمه‌ی «چک آپدیت» تنظیمات)

اگر فقط یکی/دو‌تای این‌ها آپدیت شود:
- اگر `CACHE_VERSION` در `sw.js` عقب بماند → کش قدیمی پاک نمی‌شود و کاربر فایل‌های کهنه می‌بیند.
- اگر `version.json` عقب بماند → دکمه‌ی «چک آپدیت» به‌اشتباه می‌گوید نسخه به‌روز است.
- اگر `APP_VERSION` در `script.js` عقب بماند → بج نسخه در هدر و صفحه‌ی تنظیمات عدد قدیمی نشان می‌دهد.

نکته: مقادیر ثابت `v1.1` / `1.1` در `index.html` (بج نسخه در هدر، متن نسخه در تنظیمات) صرفاً placeholder اولیه هستند و در زمان اجرا توسط `script.js` (`versionBadge` / `currentVersionText`) بازنویسی می‌شوند — نیازی به آپدیت دستی آن‌ها نیست.

## ۳. قانون ثبت Service Worker در iOS

ثبت Service Worker **همیشه** باید با `updateViaCache: 'none'` انجام شود:

```js
navigator.serviceWorker.register("sw.js", { updateViaCache: "none" });
```

**چرا:** بدون این گزینه، مرورگر (به‌خصوص Safari/iOS) فایل `sw.js` را مثل یک فایل استاتیک معمولی در کش HTTP خودش نگه می‌دارد. حتی اگر محتوای `sw.js` عوض شود، مرورگر ممکن است نسخه‌ی کهنه‌ی کش‌شده را برای چک آپدیت SW استفاده کند و هرگز متوجه نسخه‌ی جدید نشود — یعنی کل مکانیزم آپدیت اپ (که به تغییر `sw.js` وابسته است) از کار می‌افتد، بدون هیچ خطای قابل مشاهده‌ای. این تنظیم را در `script.js` هرگز حذف نکن.

## ۴. محدودیت iOS: جدایی کامل فضای Home Screen و Safari

در iOS، وقتی این PWA به صفحه‌ی هوم اضافه می‌شود (Add to Home Screen)، اپ نصب‌شده و Safari معمولی **دو origin/context کاملاً مجزا** از نظر ذخیره‌سازی محسوب می‌شوند:

- `localStorage` بین این دو فضا **مشترک نیست** — داده‌ای که در Safari ثبت شده در اپ هوم دیده نمی‌شود و برعکس.
- Service Worker و کش آن نیز مستقل هستند؛ رجیستری SW یکی در حالت standalone و دیگری در Safari.

**پیامد مهم:** پاک کردن اپ از صفحه‌ی هوم (حذف آیکون) معادل حذف کامل و برگشت‌ناپذیر داده‌های کاربر (`foods`, `groups`, `groupColors`) در آن فضاست — نه صرفاً حذف یک shortcut. قبل از هر تغییری که کاربر را به حذف/نصب مجدد اپ از هوم هدایت می‌کند (راهنما، پیام خطا، دستورالعمل آپدیت)، صراحتاً به او **یادآوری بک‌آپ‌گیری** (`btnExport` / دانلود فایل پشتیبان) بده.

## ۵. قوانین کار روی این ریپو

- **ویرایش هدفمند، نه بازنویسی کامل.** برای تغییر در `script.js` یا `sw.js` از ویرایش نقطه‌ای (تابع/بلاک مشخص) استفاده کن، نه rewrite کل فایل. فایل‌ها بدون فریم‌ورک و ماژول‌بندی هستند، پس بازنویسی کامل ریسک از دست رفتن منطق نامرتبط را بالا می‌برد.
- **قبل از commit، جاوااسکریپت را validate کن.** حداقل با `node --check script.js` و `node --check sw.js` مطمئن شو سینتکس معتبر است (اپ build/lint خودکار ندارد، پس این تنها شبکه‌ی ایمنی قبل از push است).
- **تست روی دستگاه واقعی، نه فقط دسکتاپ.** رفتارهای حیاتی این اپ (نصب PWA، به‌روزرسانی Service Worker، `updateViaCache`، جدایی فضای Home Screen/Safari) در iOS Safari دسکتاپ یا Chrome DevTools به‌درستی شبیه‌سازی نمی‌شوند؛ قبل از تأیید هر تغییر مرتبط با SW/PWA/آپدیت، روی یک آیفون واقعی (هم در Safari و هم در نسخه‌ی نصب‌شده روی هوم) تست کن.
- **تغییرات مستقیم روی برنچ `main` انجام شوند، نه روی برنچ جدا.** به‌طور پیش‌فرض commit را مستقیم روی `main` بساز و push کن. Pull Request فقط وقتی ساخته شود که کاربر صریحاً درخواست PR بدهد.
